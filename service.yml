apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80 # pod ka port 
      nodePort: 30007  # This port means on which port of the node application is exposed 
  type: NodePort # Nodeport service type